<nav class="navbar navbar-expend-lg navbar-light bg-light fixed-top">
<%= link_to 'Arabi social', root_path, class: 'navbar-brand' %>

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
 <span class="navbar-toggler-icon"></span>
 </button>

 <div class="collapse navbar-collapse flex-row-reserve" id="navbarSupportedContent">
    <ul class="navbar-nav d-flex flex-row-reserve" id="navbarSupportedContent">
    <% if user_signed_in? %>
     <li class="nav-item dropdown">
        <a class="nav-link" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">      <%= gravatar_tag current_user.email, size: 30, html ( class: 'round-circle') %>
      </a>

      <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
        <%= link_to 'الصفحة الشخصية', user_path(current_user.username), class: 'dropdown'%>
        <%= link_to'الإعدادات', edit_user_registration_path, class: 'dropdown' %>
        <div class="dropdown-divider"></div>
        <%= link_to 'تسجيل الخروج', destroy_user_session_path, method: :delete, class: 'dropdown' %>
        </div>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="#">الحائط </a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="#">المستخدمون </a>
        </li>
        <% else %>
        <li class="nav-item">
            <% link_to 'تسجيل الدخول', new_user_session_path, class: 'nav-link '%>
            </li>
            <%end%>
            </ul>
            </div>
            </nav>